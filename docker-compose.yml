services:

  user-auth:
    build: .
    container_name: user-auth-service
    ports:
      - "4000:4000"
      - "5555:5555"
      
    env_file:
      - .env
    networks:
      - dhwaniastro-net

networks:
  dhwaniastro-net:
    external: true